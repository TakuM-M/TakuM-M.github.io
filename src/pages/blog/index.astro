---
import Layout from '../../layouts/Layout.astro';
import PostList from '../../components/PostList.astro';
import { getPosts } from '../../lib/blog';

const posts = await getPosts();
---

<Layout title="Blog | Takumi">
  <h1>Blog</h1>
  <PostList posts={posts} />
</Layout>
